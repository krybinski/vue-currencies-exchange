import type { CurrencyRate } from './api/types';

export const DEFAULT_CURRENCY_CODE = 'PLN';
export const DEFAULT_TARGET_CURRENCY_CODE = 'EUR';

export const DEFAULT_CURRENCY_RATE: CurrencyRate = {
  currency: 'PLN (Polska)',
  code: 'PLN',
  mid: 1,
};
